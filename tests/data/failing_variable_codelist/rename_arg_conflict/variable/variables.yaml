# initialization fails because of conflict between pyam-region-aggregation args
# and 'region-aggregation' attribute
- Price|Carbon:
    definition: Price of carbon
    unit: USD/t CO2
    weight: Emissions|CO2
    region-aggregation:
      - Price|Carbon (Max):
          method: max
