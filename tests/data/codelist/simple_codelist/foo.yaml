- Some Variable:
    definition: Some basic variable
    unit:  # Testing that importing an empty unit (i.e. dimensionless variable) works
    bool: true  # Testing that bool is not cast to string by pydantic
    skip-region-aggregation: false
